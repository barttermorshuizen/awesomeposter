# Local development overrides
# Copy to `.env.local` and adjust as needed.

DATABASE_URL=postgresql://awesomeposter:awesomeposter@localhost:5432/awesomeposter?sslmode=disable

# Optional: override API/base URLs for local dev
# VITE_AGENTS_BASE_URL=http://localhost:3002
# API_KEY=dev-token-123
